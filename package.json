{
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "db:view": "pnpm --filter api studio"
  },
  "workspaces": [
    "api",
    "web",
    "mobile"
  ],
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
